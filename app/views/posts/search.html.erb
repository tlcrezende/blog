<h2>
    <% if @posts.any? %>
        <%= pluralize(@posts.count, 'resultado') %>:
    <% else %>
        Nenhum resultado encontrado.
    <% end %>
</h2>

<% @posts.each do |post| %>
    <%= render post %>
<% end%>